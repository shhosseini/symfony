{# src/Blogger/BlogBundle/Resources/views/Comment/form.html.twig #}

<form action="{{ path('NEWSBlogBundle_comment_create', { 'blog_id' : comment.post.id } ) }}" method="post" {{ form_enctype(form) }} class="blogger">
    {{ form_widget(form) }}
    <p>
        <input type="submit" value="Submit">
    </p>
</form>